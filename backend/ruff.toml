# Defaults only (pyflakes + pycodestyle errors). No line-length rule, so this
# stays quiet on existing code and only flags real problems.
target-version = "py312"

[lint]
select = ["E4", "E7", "E9", "F"]

